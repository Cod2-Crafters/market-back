spring:
  profiles:
    active: test
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true



file:
  base:
    dir: ${user.home}/fileStore/
